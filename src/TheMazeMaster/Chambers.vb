Friend Module Chambers
    Friend CHAMBER_MAPS As New List(Of MapData)
    Sub Initialize()
        CHAMBER_MAPS.Clear()
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER0.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER1.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER2.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER3.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER4.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER5.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER6.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER7.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER8.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER9.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER10.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER11.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER12.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER13.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER14.map"))
        CHAMBER_MAPS.Add(LOAD_RESOURCE("CHAMBER15.map"))
    End Sub
End Module
