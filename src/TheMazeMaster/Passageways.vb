Friend Module Passageways
    Friend PASSAGEWAY_MAPS As New List(Of MapData)
    Sub Initialize()
        PASSAGEWAY_MAPS.Clear()
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY0.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY1.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY2.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY3.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY4.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY5.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY6.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY7.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY8.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY9.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY10.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY11.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY12.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY13.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY14.map"))
        PASSAGEWAY_MAPS.Add(LOAD_RESOURCE("PASSAGEWAY15.map"))
    End Sub
End Module
